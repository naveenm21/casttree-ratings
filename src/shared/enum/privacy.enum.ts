export enum EPrivacy {
  Private = "Private",
  Public = "Public",
  Protected = "Protected",
}

export const ESPrivacy = ["Private", "Public", "Protected"];

export enum EStatus {
  Active = "Active",
  Inactive = "Inactive",
}

export const ESStatus = ["Active", "Inactive"];
